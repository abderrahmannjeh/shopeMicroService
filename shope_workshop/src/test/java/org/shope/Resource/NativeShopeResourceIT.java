package org.shope.Resource;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeShopeResourceIT extends ShopeResourceTest {

    // Execute the same tests but in native mode.
}